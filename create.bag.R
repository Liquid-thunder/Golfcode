# This function allows for you to create your bag
#Import the bag as two vctors one with the name of each club
# with the other having the distance of each club in their respective order

create.bag <-function(x, y){
 z <- data.frame(x, y)

  return(z)
}
